<!DOCTYPE html>
<html= lang="en">
    <!doctype html>
    <html lang="en">
      <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>HireHub</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
        <link rel="stylesheet" href="/style.css">
      </head>
<body>

  
        <!-- Navbar stars here -->

    <nav class="navbar navbar-expand-lg mb-4 bg-dark bg-gradient" style="--bs-bg-opacity: .5;">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">HireHub</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/">Home</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/jobs">jobs</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/notifications">Notifications</a>
              </li>
              <% if(curruser) { %>
              <li class="nav-item">
                <a class="nav-link " href="/jobs/new">Add Job</a>
              </li>
              <% } %>
            </ul>
            <ul class="navbar-nav mb-2 mb-lg-0">
              <% if(!curruser) { %>
                <li class="nav-item" href="/login">
                  <a class="nav-link" href="/login">Login</a>
                </li>
                <li class="nav-item" href="/register">
                  <a class="nav-link" href="/register">Register</a>
                </li>
              <% } else { %>
                <li class="nav-item" href="/user/<%= curruser._id %>">
                <a class="nav-link" href="/user/<%= curruser._id %>">Signed in as -> <%= curruser.username %> <- </a>

                </li>
                <li class="nav-item" href="/logout">
                  <a class="nav-link" href="/logout">Logout</a>
              </li>
              <% }%>
              </ul>
          </div>
        </div>
      </nav>

            <!-- Navbar Ends here -->